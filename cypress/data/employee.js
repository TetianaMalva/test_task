export default class Employee {
    
    constructor(firstName, lastName, startDate, email) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.startDate = startDate;
        this.email = email;
    }
}